<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <!-- Only root may own the name: the service is the authority on who may use
       the camera, the microphone and the user's accounts, so a program that
       could claim the name would answer those questions itself. -->
  <policy user="root">
    <allow own="ar.net.vasak.os.Permissions"/>

    <!-- Y el servicio tiene que poder hablarle al agente.

         Sin esta línea no podía, y eso rompía **todo el flujo de preguntar**:
         en el bus del sistema los method calls se deniegan por omisión, y acá
         sólo estaba permitido enviar *hacia* el servicio. El servicio llamando
         al agente —que es la dirección contraria— quedaba denegado con
         «Sender is not authorized to send message», así que ni el diálogo de
         permisos ni el aviso de bloqueo llegaban nunca a aparecer.

         No se pone `send_destination` porque el agente no posee un nombre
         conocido: se registra y conversa por su nombre único, que cambia en
         cada arranque. La interfaz alcanza para acotarlo, y esto sólo se lo
         permite a root, que es quien corre el servicio. -->
    <allow send_interface="ar.net.vasak.os.PermissionAgent"/>
  </policy>

  <!-- Anyone may ask. Asking is the whole point, and every method decides for
       itself what the caller is entitled to: CheckPermission consults the
       stored policy, the management methods go through polkit, and
       RegisterAgent only accepts the installed agent binary. -->
  <policy context="default">
    <allow send_destination="ar.net.vasak.os.Permissions"
           send_interface="ar.net.vasak.os.Permissions"/>
    <allow send_destination="ar.net.vasak.os.Permissions"
           send_interface="org.freedesktop.DBus.Introspectable"/>
    <allow send_destination="ar.net.vasak.os.Permissions"
           send_interface="org.freedesktop.DBus.Properties"/>
  </policy>
</busconfig>
